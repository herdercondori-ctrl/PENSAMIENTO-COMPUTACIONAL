// Online C++ compiler to run C++ program online
#include <iostream>
#include<cmath>
using namespace std;
int main() {
float x,y,z,n;
cin >>x>>y>>z;
n=((((2*x)+y)/z)*((y*y*y)-z))/((x+(2*y)+(3*z))/(z-(2*y)-(3*x))+(x*x)+(z*z));
  //n es igual
  cout<<n;
    return 0;
}